# Runtime dependencies for the backend (FastAPI + in-process 11-agent pipeline).
# Generated from pyproject.toml [project.dependencies]. Keep the two in sync.
# System packages required alongside these (installed via apt in the Dockerfile):
#   tesseract-ocr   -> pytesseract (local OCR, primary engine)
#   libzbar0        -> pyzbar (QR decoding)
#   libgl1, libglib2.0-0 -> opencv-python-headless runtime libs

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic[email]>=2.6
pydantic-settings>=2.2
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29                 # async engine: FastAPI request handlers
psycopg2-binary>=2.9.9       # sync engine: 11-agent BackgroundTasks pipeline (app/core/database.py). cp312 manylinux wheels, no build tools needed.
alembic>=1.13
argon2-cffi>=23.1
pyjwt>=2.8
python-multipart>=0.0.9
aiofiles>=23.2
opencv-python-headless>=4.9
pyzbar>=0.1.9
pytesseract>=0.3.13
pymupdf>=1.24
pdfplumber>=0.11
pillow>=10.3
rapidfuzz>=3.6
numpy>=1.26
httpx>=0.27
structlog>=24.1
prometheus-fastapi-instrumentator>=6.1
